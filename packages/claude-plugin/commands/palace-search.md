---
description: Search Memory Palace for relevant context
allowed-tools: ["mcp__memory-palace__palace_search"]
---

Search the Memory Palace for context related to: $ARGUMENTS

Use the `palace_search` MCP tool with the user's query. Present results in a concise format showing:
- Memory short_id
- Session name and agent
- Key content (decisions, built items, next steps)
- Similarity score

If no arguments provided, ask the user what they'd like to search for.

After showing results, briefly note any memories that seem particularly relevant to current work context.
